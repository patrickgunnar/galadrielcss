/**
 * Generate class names from a record of classes.
 *
 * @param classes - The record of classes, where keys are class names, and values can be strings or objects.
 * @returns The generated class names as a string.
 */
export function genClassNames(classes: Record<string, any>): string;
